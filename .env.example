# Copia este archivo a .env.local y rellena los valores
# Para Vercel: configura estas mismas variables en Project Settings → Environment Variables

# Google Gemini (free tier) — uso futuro en /api/chat
GEMINI_API_KEY=
